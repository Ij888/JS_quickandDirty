function loopty(){
    setInterval(function(){ 
        document.getElementById("prgrs").value += 1;
    }, 1000);
        
      
 }

//how setInterval works: function to be executed, interval to between which to run fn