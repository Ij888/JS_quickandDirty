
var dataStore = {};
dataStore.d1 = 1024;
dataStore.d2 = 0.512;
dataStore.d3 = 3553;

//return dataStore.d2;
/*
a return statement can only be used in a function, rather use 
console.log to output to a nodeJS shell
*/

/*
also not that the "value" in key:value pairings in objects must
be declared if it is not a number
*/

console.log(dataStore.d2);